import React, { useState } from 'react'

export const Sateconcept = () => {
    const [count,setcount]=useState(0);

  return (
    <div>
        <button onClick={()=>setcount(count+1)}>Increase Count </button>
      <p>count {count}
        </p>
        df, df,.d fl;dmf;ldfdl;fmdf


    </div>
  )
}
